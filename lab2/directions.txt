Write an assembly language program that prompts the user for two input strings and
computes the Hamming distance between the two strings. The Hamming distance is the
number of bit positions where the two strings differ. For example, the ASCII representations
of the strings “foo” and “bar” in binary are: “foo” = 0110 0110 0110 1111 0110 1111
“bar” = 0110 0010 0110 0001 0111 0010
So, the Hamming distance between “foo” and “bar” is 8.
    • If the user enters two strings with different lengths, your program should return the
Hamming distance up to the length of the shorter string.
    • The maximum length of the string could be 255 characters.
    • The Hamming distance between the following two strings is 38:
“this is a test” and “of the emergency broadcast”
    •  You must also make your own test cases. We will test it with ours!



    Linux read and write system calls:
Register: write, read
RAX: 1 = write, 0 = read
RDI: 1 = stdout, 0 = stdin
RSI: Address of what to print, Address where input will be stored
RDX: Number of bytes to print, Maximum number of bytes to read


Linux write system call
RAX = 1 #write
RDI = 1 #stdout
RSI = msg # what to print
RDX = len # how much to print


xor %ch,%ah # ah=ah XOR ch